export const FREQUENCY_MS = 250
export const LOCAL = "local"

export enum THEME {
  dark = "dark",
  light = "light",
}

export enum STATUS {
  idle = "idle",
  pending = "pending",
  succeeded = "succeeded",
  failed = "failed",
}
